import { h, toChildArray } from "preact";
import styles from "./inline.css";
export function Inline({ children, space, ...rest }) {
    return (h("div", { ...rest, class: typeof space === "undefined" ? undefined : styles[space] }, toChildArray(children).map(function (element, index) {
        return (h("div", { key: index, class: styles.item }, element));
    })));
}
//# sourceMappingURL=inline.js.map