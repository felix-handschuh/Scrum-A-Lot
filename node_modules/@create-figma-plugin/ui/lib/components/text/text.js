import { h } from "preact";
import { createClassName } from "../../utilities/create-class-name.js";
import styles from "./text.css";
export function Text({ align = "left", bold = false, children, muted = false, numeric = false, ...rest }) {
    return (h("div", { ...rest, class: createClassName([
            styles.text,
            styles[align],
            bold === true ? styles.bold : null,
            muted === true ? styles.muted : null,
            numeric === true ? styles.numeric : null
        ]) }, children));
}
//# sourceMappingURL=text.js.map