import { h } from "preact";
import { createClassName } from "../../../utilities/create-class-name.js";
import { RawTextbox } from "./private/raw-textbox.js";
import styles from "./textbox.css";
export function Textbox({ icon, noBorder = false, ...rest }) {
    if (typeof icon === "string" && icon.length !== 1) {
        throw new Error(`String \`icon\` must be a single character: ${icon}`);
    }
    return (h("div", { class: createClassName([
            styles.textbox,
            noBorder === true ? styles.noBorder : null,
            typeof icon === "undefined" ? null : styles.hasIcon,
            rest.disabled === true ? styles.disabled : null
        ]) }, h(RawTextbox, { ...rest, class: styles.input }), typeof icon === "undefined" ? null : (h("div", { class: styles.icon }, icon)), h("div", { class: styles.border })));
}
//# sourceMappingURL=textbox.js.map