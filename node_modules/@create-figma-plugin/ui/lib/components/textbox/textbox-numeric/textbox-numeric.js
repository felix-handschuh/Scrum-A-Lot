import { h } from "preact";
import { createClassName } from "../../../utilities/create-class-name.js";
import textboxStyles from "../textbox/textbox.css";
import { RawTextboxNumeric } from "./private/raw-textbox-numeric.js";
export function TextboxNumeric({ icon, noBorder = false, ...rest }) {
    if (typeof icon === "string" && icon.length !== 1) {
        throw new Error(`String \`icon\` must be a single character: ${icon}`);
    }
    return (h("div", { class: createClassName([
            textboxStyles.textbox,
            noBorder === true ? textboxStyles.noBorder : null,
            typeof icon === "undefined" ? null : textboxStyles.hasIcon,
            rest.disabled === true ? textboxStyles.disabled : null
        ]) }, h(RawTextboxNumeric, { ...rest, class: textboxStyles.input }), typeof icon === "undefined" ? null : (h("div", { class: textboxStyles.icon }, icon)), h("div", { class: textboxStyles.border })));
}
//# sourceMappingURL=textbox-numeric.js.map