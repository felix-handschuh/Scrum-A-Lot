import { ComponentChildren, JSX } from 'preact';
import { Props } from '../../types/types';
export declare type ButtonProps = {
    children: ComponentChildren;
    destructive?: boolean;
    disabled?: boolean;
    fullWidth?: boolean;
    loading?: boolean;
    onClick?: JSX.MouseEventHandler<HTMLButtonElement>;
    propagateEscapeKeyDown?: boolean;
    secondary?: boolean;
};
export declare function Button({ children, destructive, disabled, fullWidth, loading, onClick, propagateEscapeKeyDown, secondary, ...rest }: Props<HTMLButtonElement, ButtonProps>): JSX.Element;
//# sourceMappingURL=button.d.ts.map