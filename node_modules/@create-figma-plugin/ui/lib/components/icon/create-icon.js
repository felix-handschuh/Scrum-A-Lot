import { h } from "preact";
import styles from "./icon.css";
export function createIcon(path, options) {
    const { width, height } = options;
    return function Icon({ color, ...rest }) {
        return (h("svg", { ...rest, class: typeof color === "undefined" ? styles.currentColor : styles[color], height: height, width: width, xmlns: "http://www.w3.org/2000/svg" }, h("path", { "clip-rule": "evenodd", d: path, "fill-rule": "evenodd" })));
    };
}
//# sourceMappingURL=create-icon.js.map